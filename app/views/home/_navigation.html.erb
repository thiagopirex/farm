<nav class="navbar navbar-inverse">
  <div class="container">
  	<% if logged_in? %>
  	<!-- com autenticacao -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="/"><%= t('home') %></a>
    </div>
    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Propriedades<span class="caret"></span></a>
          <ul class="dropdown-menu">
          	<% @propriedades.each do |p| %>
          		<li><%= link_to p.nome, p%></li>
          	<% end %>
            <li role="separator" class="divider"></li>
            <li><%= link_to 'Nova Propriedade', new_propriedade_path %></li>
            <li role="separator" class="divider"></li>
            <!--
            	<li><a href="#">One more separated link</a></li>
             -->
          </ul>
        </li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
      	<form class="navbar-form navbar-left">
	        <div class="form-group" style="color: white; font-size: 18px;">
	          <%= if !@propriedade.nil? && !@propriedade.id.nil?
	          	 link_to @propriedade.nome, @propriedade, class:"btn btn-warning btn-md"
	          	end %>
	        </div>
	      </form>
	      <li><%= link_to 'Sair', logout_path, class: "navbar-brand" %></li>
      </ul>
      
      
    </div>
    <!-- /.navbar-collapse -->
    
	<% else %>
	<!-- sem autenticacao -->
	<div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-right">
		<li><%= link_to 'Entrar', login_path, class: "navbar-brand" %></li>
	  </ul>
	</div>
	<% end %>
  </div><!-- /.container-fluid -->
</nav>
